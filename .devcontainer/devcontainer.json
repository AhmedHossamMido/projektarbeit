{
  "name": "SciPro Environment",
  "build": {
    "dockerfile": "Dockerfile"
  },
  "postCreateCommand": "pip install --no-cache-dir -r requirements.txt && git config --global user.name 'AhmedHossamMido' && git config --global user.email 'a@b.com'",
  "customizations": {
    "vscode": {
      "extensions": [
        "ms-python.python",
        "ms-toolsai.jupyter"
      ]
    }
  },
  "remoteUser": "jovyan",
  "settings": {
    "terminal.integrated.defaultProfile.linux": "bash"
  }
}
